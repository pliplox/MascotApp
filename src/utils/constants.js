/**
 * This is to keep query keys made by swr library centralized in one place
 * and use them when is needed to refetch by adding, updating and/or removing something
 *
 * When adding more keys, please try to keep this object by alphabetic order
 */
export const queryKeys = {
  groupList: 'family/groups',
}
